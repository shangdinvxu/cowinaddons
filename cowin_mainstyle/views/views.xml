<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cowin_mainstyle.list">
      <field name="name">cowin_mainstyle list</field>
      <field name="model">cowin_mainstyle.cowin_mainstyle</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cowin_mainstyle.action_window">
      <field name="name">cowin_mainstyle window</field>
      <field name="res_model">cowin_mainstyle.cowin_mainstyle</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cowin_mainstyle.action_server">
      <field name="name">cowin_mainstyle server</field>
      <field name="model_id" ref="model_cowin_mainstyle_cowin_mainstyle"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cowin_mainstyle" id="cowin_mainstyle.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cowin_mainstyle.menu_1" parent="cowin_mainstyle.menu_root"/>
    <menuitem name="Menu 2" id="cowin_mainstyle.menu_2" parent="cowin_mainstyle.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cowin_mainstyle.menu_1_list" parent="cowin_mainstyle.menu_1"
              action="cowin_mainstyle.action_window"/>
    <menuitem name="Server to list" id="cowin_mainstyle" parent="cowin_mainstyle.menu_2"
              action="cowin_mainstyle.action_server"/>
    -->
  </data>
</odoo>