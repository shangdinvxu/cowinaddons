<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cowin_settings.list">
      <field name="name">cowin_settings list</field>
      <field name="model">cowin_settings.cowin_settings</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cowin_settings.action_window">
      <field name="name">cowin_settings window</field>
      <field name="res_model">cowin_settings.cowin_settings</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cowin_settings.action_server">
      <field name="name">cowin_settings server</field>
      <field name="model_id" ref="model_cowin_settings_cowin_settings"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cowin_settings" id="cowin_settings.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cowin_settings.menu_1" parent="cowin_settings.menu_root"/>
    <menuitem name="Menu 2" id="cowin_settings.menu_2" parent="cowin_settings.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cowin_settings.menu_1_list" parent="cowin_settings.menu_1"
              action="cowin_settings.action_window"/>
    <menuitem name="Server to list" id="cowin_settings" parent="cowin_settings.menu_2"
              action="cowin_settings.action_server"/>
    -->
  </data>
</openerp>